<view class="promotional-section-components">
  <!-- 宣传板块 -->
  <view
    class="promotional-section-item"
    wx:for="{{activeList}}"
    data-id="{{item.id}}" 
    data-item="{{item}}"
    wx:key="index"
    bind:tap="onProductClick">
    <image src="{{item.pic}}"/>
  </view>
</view>