<view class="xxx-page">
  <!-- content -->
  <page-container pageName="pageList">

      <view
        wx:for="{{pageList}}"
        data-url="{{item}}"
        bind:tap="toPage"
        style="margin: 20px"
      >{{item}}</view>
  
  </page-container>
</view>